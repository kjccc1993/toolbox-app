//Por lo general estos datos deben venir de variables de ambiente (.env),
//se deja así porque es requisito para la prueba
const env = {
  apiUrl: 'http://localhost:3333/files',
}

export default env
